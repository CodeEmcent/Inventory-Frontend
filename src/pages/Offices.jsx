import React from "react";
import { Typography } from "@mui/material";

const Offices = () => {
    return (
        <div>
            <Typography variant="h5">Offices</Typography>
            <Typography>Manage offices in your organization here.</Typography>
        </div>
    );
};

export default Offices;
