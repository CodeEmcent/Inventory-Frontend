import React from "react";
import { Typography } from "@mui/material";

const Inventory = () => {
    return (
        <div>
            <Typography variant="h5">Inventory</Typography>
            <Typography>Track and manage inventory items here.</Typography>
        </div>
    );
};

export default Inventory;
